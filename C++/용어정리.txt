SDL(Security Development Lifecycle)검사:
속성>C++>일반>SDL검사>아니오(실제 빌드환경에서는 권장 불가)

함수의선언: 함수를 호출하기위해서 함수이름,리턴값,매개변수를 선언하여 함수가 미리 있음을 알린다.
ㄴ없다면, 식별자를 찾을수없다는 컴파일에러 발생
선언: 미리 있음을 알리는 것.
식별자: 구분하기위한 것.
함수의정의: 함수가 어떤값을 연산하여 리턴하는지 내용을 정의하여 사용 할수 있게 준비해주는 것.
ㄴ없다면, 외부기호를 찾을수없다는 컴파일에러 발생
외부기호: 선언된 대상을 정의하여 사용할수 있게 만들어주는 것.
호출: 사용하기위해서 부르는 것.
[프로그래밍기초]
변수:(메모리(RAM)에 할당되어) 데이터를 담는 공간. 
포인터: 변수의 주소값을 저장하는 변수.
매개변수:
동적할당:
정적할당:

void포인터: 형태가 없는 포인터(RAM의 시작주소). 어떤 메모리의 주소값이든 저장가능한 포인터. 다만 포인터에 접근해서 사용은 불가능하다. 크기는 컴파일된 비트수에 따라 달라진다. (64bit -> 8, 32bit -> 4) 
int포인터: 시작주소값에서 +4만큼 사용한 변수.
double포인터: 시작주소에서 +8만큽 사용하는 변수.
struct포인터: 시작주소에서 +모든멤버의 크기만큼 변수 예) LinkedList: SNode(int data, SNode* pNext) x8 / Graph: SNode(char cData, SNode* pLeft, SNode* pRight) -> 9? -> 12
arg:


[객체지향]
인터페이스:

오픈소스라이브러리:
ㄴ오픈소스: 누구나 보고 사용 할 수 있는 소스코드
ㄴ라이브러리:

[운영체제]
플랫폼설정
x64: 크기 2^64 = ?? 100만t
x86: 파일1개의 크기 2^32 = 4g  최대 메모리 4g -> 포인터의 최대크기 2^32 롱포인터(Lptr)
ARM:
